# Dependencias de PRODUCCIÓN para el paquete Lambda — versiones pinneadas (==)
# al entorno real instalado (mismas versiones que requirements.txt).
#
# Excluye respecto a requirements.txt:
#   - ragas, datasets          → solo para evaluación local
#   - pytest, pytest-asyncio, pytest-cov → solo para tests locales
# Paquetes pesados transitivos (onnxruntime, huggingface_hub, tokenizers, pyarrow,
# grpcio, etc.) se excluyen en build time dentro de scripts/deploy_localstack.py.

# ── API ───────────────────────────────────────────────────────────────────────
fastapi==0.136.1
uvicorn==0.47.0
mangum==0.21.0

# ── AWS / Cloud ───────────────────────────────────────────────────────────────
boto3==1.43.31

# ── Config ────────────────────────────────────────────────────────────────────
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2

# ── LangChain / LangGraph (major 1.x — set real instalado) ────────────────────
langchain==1.3.1
langgraph==1.2.1
langchain-openai==1.2.2
langchain-community==0.4.2
langchain-text-splitters==1.1.2

# ── Vectores / RAG ────────────────────────────────────────────────────────────
chromadb==1.5.9
pinecone==9.1.0

# ── Herramientas del agente ───────────────────────────────────────────────────
requests==2.34.2
duckduckgo-search==8.1.1
tiktoken==0.13.0

# ── Pins de deps transitivas (resolución de conflictos), al set real instalado ─
instructor==1.15.1
rich==14.3.4
fsspec==2026.2.0
